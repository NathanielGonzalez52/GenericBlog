<%- include('partials/header') %>


<h1>Home</h1>

<p> <%= homeStartingContent %> </p>

<% posts.forEach(function(post) { %>

  <h2> <%= post.title %> </h2>

  <p class="post"> <%= post.content %> </p>
  <a class="read" href="/home/<%= post.title %>">Read More</a>

  <% }) %>

<%- include('partials/footer') %>
